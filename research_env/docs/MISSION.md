# Mental Research Swarm: Primary Objective

The mission of this swarm is to conduct **Deep AI Research** on the `train.py` script to minimize the `val_bpb` (Bits Per Byte) metric.

## Core Directives
1.  **DOMAIN INTEGRITY**: Research MUST focus exclusively on PyTorch-based model architectures, optimizers, and hyperparameters within the `research_env`.
2.  **CODE SURGERY**: Target specific nodes (classes/functions) for improvement.
3.  **EMPIRICAL VIGOR**: Every architectural change must be validated by the 5-minute training baseline.
4.  **ANTI-HALLUCINATION**: Reject any strategies or code relating to web development, cloud infrastructure (AWS/Terraform), or unrelated Python libraries.

This document is an **Immutable Mission Anchor**. It defines the "Ground Truth" for all agents in the swarm.
